**/node_modules
**/dist
client/src/schema.d.ts
server/src/api.d.ts
pnpm-lock.yaml
pnpm-workspace.yaml
**/database.sqlite

# Generierte Playwright-Ausgaben (frontend/.gitignore) — Prettier respektiert .gitignore nicht
# automatisch, der neue root-weite lefthook-Check (format-root) wuerde sonst auf Artefakten
# fremder lokaler Testlaeufe fehlschlagen statt auf echten Formatierungsfehlern.
**/test-results
**/playwright-report
**/blob-report
**/dev-dist

# Nur lokal via globalem git-ignore ausgeschlossen, nicht im Repo selbst — vorsichtig auch
# hier, damit format-root nicht auf einer Maschine ohne diese globale Regel bricht.
.claude/settings.local.json

# Prompt-Templates für die CI-Workflows (geladen via cat+sed). Prettier würde den Prompt-
# Inhalt korrumpieren: Path-Globs wie *.test.ts werden zu _.test.ts (Emphasis-Normalisierung),
# und die relative Einrückung der ABLAUF-Schritte wird plattgedrückt. Prompt-Text ist kein
# Prosa-Markdown — bewusst nicht formatiert.
.github/prompts/

# Third-Party-Design-Skill (Impeccable, via `npx impeccable install` eingesetzt und per
# `npx impeccable update` aktualisiert, #828): SKILL.md/Referenzen/Scripts sind Fremd-Artefakte —
# Formatierung wäre beim nächsten Update sofort wieder weg und würde den Format-Check brechen.
.claude/skills/
