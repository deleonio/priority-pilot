{
	"devDependencies": {
		"npm-check-updates": "22.2.3",
		"prettier": "3.8.4"
	},
	"private": true,
	"type": "module",
	"scripts": {
		"build": "pnpm -r generate && pnpm -r build",
		"format": "prettier --write .",
		"lint": "pnpm -r lint",
		"update": "ncu -u && pnpm -r exec ncu -u && pnpm install"
	}
}
