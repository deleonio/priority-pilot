{
	"version": "0.3.4",
	"license": "EUPL-1.2",
	"devDependencies": {
		"@action-validator/core": "0.6.0",
		"@evilmartians/lefthook": "2.1.14",
		"knip": "6.37.0",
		"prettier": "3.9.8",
		"tsx": "4.23.13"
	},
	"private": true,
	"type": "module",
	"packageManager": "pnpm@12.5.1",
	"scripts": {
		"build": "pnpm -r generate && pnpm -r build",
		"cost:record": "node .github/scripts/cost-from-transcript.ts",
		"cost:report": "node .github/scripts/turns-report.ts --dir .costs",
		"dev": "pnpm -r --parallel dev",
		"format": "prettier . --write --ignore-unknown",
		"knip": "pnpm -r knip",
		"lint": "pnpm -r lint",
		"lint:actions": "tsx .github/scripts/validate-actions.ts",
		"prepare": "lefthook install",
		"test": "pnpm -r test && pnpm test:scripts",
		"test:scripts": "node --import tsx --test \".github/scripts/*.test.ts\"",
		"ui:inspect": "./ui-inspect.sh"
	}
}
