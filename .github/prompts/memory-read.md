READ MEMORY FROM `.ai-memory/` (TWO levels, status: {{MEMORY_STATUS}}):

1. **`MEMORY.md`** — PERSISTENT MEMORY, cross-issue, ALWAYS READ FIRST (even on the first run). Contains mistakes from earlier runs → do NOT repeat them, reuse the solutions.
2. **This issue's phase notes** (`.ai-memory/issue-*.md`) are already in the "KONTEXT AUS DEN VORHERIGEN PHASEN" (context from previous phases) block. You don't need to re-read them. Block missing = first phase → start normally.

**HOW TO USE THE BLOCK**:
- Erledigt (done) → do NOT do it again
- Verworfen (discarded) → do NOT try it again (the reason is included)
- Relevante Stellen (relevant spots) → your starting list
- Nächster Schritt (next step) → your task for the same phase

**DON'T TRUST FILE STATES BLINDLY**: notes may come from a different branch → cross-check statements; assumptions/decisions still hold.
